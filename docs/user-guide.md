# Rosetta 用户手册

## 登录

浏览器打开 `http://localhost:8080`，使用系统管理员分配的账号登录。

登录后进入仪表盘，显示各模块数据概览。

---

## 功能模块

### 1. 仪表盘

首页展示 4 张统计卡片：用户数、实例数、字典数、模型数。点击卡片可跳转到对应管理页。

### 2. 用户管理

路径：`#/users`

| 操作 | 说明 |
|------|------|
| 新建用户 | 点击「+ 新建用户」，填写用户名、密码、显示名，勾选角色 |
| 编辑用户 | 点击「编辑」，修改显示名、邮箱、角色 |
| 分配角色 | 点击「角色」，勾选要分配的角色 |
| 重置密码 | 编辑弹窗中点击「重置密码」，输入新密码 |
| 启/禁用 | 点击「禁用」或「启用」切换状态 |

系统预置 4 个角色：
- **超级管理员** — 所有权限
- **数据治理管理员** — 全局可读
- **数据开发** — 被授权模型可读写
- **数据分析** — 被授权模型只读

### 3. 实例管理

路径：`#/instances`

管理数据库连接信息。支持 MySQL 和 GaussDB M 两种类型。

| 操作 | 说明 |
|------|------|
| 注册实例 | 填写名称、类型、主机、端口、用户名、密码、数据库 |
| 编辑实例 | 修改连接信息 |
| 删除实例 | 确认后删除，关联的 Schema 一并删除 |
| 管理 Schema | 点击「Schema」，查看已有 Schema，可新建 |

Schema 层级：ODS（贴源层）、DWD（明细层）、DWS（汇总层）、ADS（应用层）。

### 4. 字典维护

路径：`#/dicts`

统一管理业务术语和数据标准。

| 操作 | 说明 |
|------|------|
| 新建字典 | 填写名称、编码，选择类型 |
| 编辑/删除 | 在右侧详情区顶部操作 |
| 添加条目 | 选中字典后点击「+ 添加条目」 |
| 编辑条目 | 点击条目行的「编辑」 |
| 删除条目 | 点击条目行的「删除」 |

三种字典类型：
- **标准字典** — 业务术语，如"用户状态：启用/禁用/注销"
- **类型映射** — 逻辑类型与数据库物理类型对应关系
- **参考数据** — 系统级枚举，如"性别""是否删除"

### 5. 模型管理

路径：`#/models`

这是平台核心功能，定义数据表结构。

#### 5.1 模型列表

- 展示所有逻辑模型，支持按表名搜索
- 点击模型名进入详情编辑
- 点击「DDL」弹窗预览双方言 DDL

#### 5.2 模型详情编辑

4 个 Tab 页：

**字段**：添加/编辑/删除字段。每个字段配置：
- 字段名（snake_case）
- 逻辑类型（BIGINT、VARCHAR、DECIMAL、DATETIME 等）
- 长度/精度
- 是否可为空
- 是否主键
- 默认值
- 注释

**索引**：添加/删除索引，配置索引列和排序方向。

**外键**：添加/删除外键约束，指定引用模型和引用列。

**DDL 预览**：实时切换 MySQL / GaussDB M 查看生成的建表语句。

#### 5.3 DDL 方言差异

| 特性 | MySQL | GaussDB M |
|------|-------|-----------|
| 自增主键 | `AUTO_INCREMENT` | `GENERATED BY DEFAULT AS IDENTITY` |
| 日期时间 | `DATETIME` | `TIMESTAMP` |
| 大文本 | `TEXT` | `CLOB` |
| 布尔 | `TINYINT(1)` | `BOOLEAN` |
| 列注释 | 内联 `COMMENT 'xxx'` | 独立 `COMMENT ON COLUMN` |

---

## 典型操作流程

### 场景：设计一张新表并生成 DDL

1. **创建模型** — `模型管理` → `+ 新建模型`，输入表名如 `user_order`
2. **添加字段** — 点击模型名进入详情 → Tab「字段」→ `+ 添加字段`
3. **添加索引** — Tab「索引」→ `+ 添加索引`
4. **添加外键** — Tab「外键」→ `+ 添加外键`（引用另一个模型）
5. **预览 DDL** — Tab「DDL 预览」，切换 MySQL/GaussDB 查看
6. **复制使用** — 点击「复制」或直接复制 SQL 文本

### 场景：反向工程已有表

1. **注册实例** — `实例管理` → `+ 注册实例`，填写数据库连接信息
2. **管理 Schema** — 为实例创建 Schema（如 ods）
3. **导入表** — 通过 API 调用反向工程接口：

```bash
# 获取 Token
TOKEN=$(curl -s http://localhost:8080/api/auth/login \
  -H "Content-Type: application/json" \
  -d '{"username":"<用户名>","password":"<密码>"}' \
  | python3 -c "import sys,json; print(json.load(sys.stdin)['data']['token'])")

# 浏览表
curl -s "http://localhost:8080/api/instances/1/tables?schema=ods" \
  -H "Authorization: Bearer $TOKEN"

# 预览
curl -s http://localhost:8080/api/models/reverse-engineer \
  -H "Authorization: Bearer $TOKEN" \
  -H "Content-Type: application/json" \
  -d '{"instance_id":1,"schema_name":"ods","table_name":"my_table"}'

# 导入
curl -s http://localhost:8080/api/models/reverse-engineer/import \
  -H "Authorization: Bearer $TOKEN" \
  -H "Content-Type: application/json" \
  -d '{"instance_id":1,"schema_name":"ods","table_name":"my_table"}'
```

---

## 常见问题

**Q: 登录失败？**
确保 MySQL 容器正在运行：`docker compose ps`。确认密码正确。

**Q: 页面空白？**
检查浏览器控制台是否有 JS 错误。刷新页面重试。

**Q: 字段的"可为空"不生效？**
编辑字段后点击保存，模型详情页会重新加载。

**Q: 如何切换 MySQL / GaussDB DDL 预览？**
在模型详情的「DDL 预览」Tab 或列表页的「DDL」弹窗中，点击 Tab 按钮切换。

**Q: 如何添加 GaussDB 实例？**
实例类型选择「GaussDB M」，填写 GaussDB 的连接信息。

**Q: 字典的类型映射如何使用？**
创建 `TYPE_MAPPING` 类型的字典，条目编码为逻辑类型名（如 `VARCHAR`），条目名称为物理类型（如 `VARCHAR(255)`），后续 DDL 渲染引擎会读取这些映射。
